import React from 'react'
import { View, Text } from "react-native";
export class NotificationActivity extends React.Component{

    render(){
        return (
            <View style={{justifyContent:'center', alignItems: 'center',}}>
                <Text>Notifications are yet to come...</Text>
            </View>
        );
    }
}